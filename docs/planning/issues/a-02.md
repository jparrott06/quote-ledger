## Context

Epic **A — Domain model**: clarify ownership of monotonic `seq` and ordering guarantees.

## Requirements

- Document that **`seq` is assigned only by the write path / storage layer** (Epic B), not inside `reduce`.
- Document per-`quote_id` ordering: events are strictly ordered; reducer assumes commit order.

## Acceptance criteria

- [ ] Module-level docs in `domain` (or linked README section) state the above unambiguously.
- [ ] No behavior change required unless current code contradicts the docs.
- [ ] CI green.

## Edge cases & failure modes

| Scenario | Expected behavior |
|----------|---------------------|
| Out-of-order delivery at domain boundary | Documented as storage/transport bug, not reducer responsibility |

## QA / test plan

**Automated**

- N/A (documentation), or a doc-test if you add one.

**Manual**

- Reviewer reads `src/domain/mod.rs` and agrees scope is clear.

## Rollout / compatibility

Docs only.

## Out of scope

- Implementing `seq` assignment.
